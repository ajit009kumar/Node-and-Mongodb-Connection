input {
   file {
     path => "C:\Users\MYANATOMY\Downloads\node-todo-api\output_test1.csv"
     start_position => "beginning"
     sincedb_path => "/dev/null" 

  }
}


filter {

    csv {

       columns => ["name", "age", "rollNo" ]
        separator => ","
       
    }

      mutate { convert => [ "age", "integer"] }
      mutate { convert => [ "rollNo", "integer"] }
}
 




output {

   elasticsearch { 
        hosts => "localhost:9200" 
        index => "candidate"
        document_type => "stud"
       document_id => "%{name}"
    }

     stdout {}
}               

